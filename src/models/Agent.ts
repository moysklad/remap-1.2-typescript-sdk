/* tslint:disable */
/* eslint-disable */
/**
 * МойСклад JSON API
 * API для манипуляции с сущностями и создания отчетов в онлайн-сервисе МойСклад  ## Аутентификация  МойСклад поддерживает аутентификацию по протоколу Basic Auth и с использованием токена доступа: - Basic Auth: заголовок `Authorization` со значением пары `логин:пароль`, закодированным в Base64 - Bearer Token: заголовок `Authorization` со значением `Bearer <Access-Token>`  ## Ограничения  - Не более 45 запросов за 3 секундный период от аккаунта - Не более 5 параллельных запросов от одного пользователя   - Не более 20 параллельных запросов от аккаунта - Не более 20 Мб данных в одном запросе - Максимум 1000 элементов в массиве - Обязательное использование сжатия gzip 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import * as AgentPolymorphicParent from './EntityWithMeta.js';
import * as AgentPolymorphicCounterparty from './Counterparty.js';
import * as AgentPolymorphicOrganization from './Organization.js';
import * as AgentPolymorphicEmployee from './Employee.js';
/**
 * Метаданные контрагента или юрлица. Используется как тип полей-ссылок,
 * которые принимают только meta-ссылку, а в ответе сервера могут вернуться
 * как Counterparty, так и Organization. На запись поля `meta` достаточно, остальные поля
 * Counterparty/Organization читаются сервером только при создании самих этих сущностей.
 * 
 * @export
 * @interface Agent
 */
export interface AgentOwn {
    /**
     * ID товара
     * @type {string}
     * @memberof Agent
     */
    readonly id?: string;
}
export type Agent = AgentOwn & AgentPolymorphicParent.EntityWithMeta;


function readAgentPolymorphicDiscriminator(value: any): unknown {
    const path = 'meta.type'.replace(/^\//, '').replace(/\//g, '.');
    return path.split('.').filter(Boolean).reduce(
        (current: any, segment: string) => current == null ? undefined : current[segment],
        value,
    );
}

function isAgentPolymorphicBatchError(value: any): boolean {
    return Array.isArray(value?.errors)
        && value.errors.length > 0
        && typeof value.errors[0]?.error === 'string';
}

/**
 * Check if a given object implements the Agent interface.
 */
export function instanceOfAgent(value: object): value is Agent {
    return true;
}

export function AgentFromJSON(json: any): Agent {
    return AgentFromJSONTyped(json, false);
}

export function AgentFromJSONTyped(json: any, ignoreDiscriminator: boolean): Agent {
    if (json == null) {
        return json;
    }
    if (!ignoreDiscriminator) {
        const discriminator = readAgentPolymorphicDiscriminator(json);
        const converters: Record<string, (value: any) => any> = {
            'counterparty': AgentPolymorphicCounterparty.CounterpartyFromJSON,
            'organization': AgentPolymorphicOrganization.OrganizationFromJSON,
            'employee': AgentPolymorphicEmployee.EmployeeFromJSON,
        };
        const converter = typeof discriminator === 'string'
            ? converters[discriminator] ?? converters['undefined']
            : undefined;
        if (converter !== undefined) {
            return converter(json);
        }
    }
    return {
        ...AgentPolymorphicParent.EntityWithMetaFromJSONTyped(json, true),
        'id': json['id'] == null ? undefined : json['id'],
    };
}

export function AgentToJSON(json: any): Agent {
    return AgentToJSONTyped(json, false);
}

export function AgentToJSONTyped(value?: Omit<Agent, 'id'> | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }
    if (!ignoreDiscriminator) {
        const discriminator = readAgentPolymorphicDiscriminator(value);
        const converters: Record<string, (item: any) => any> = {
            'counterparty': AgentPolymorphicCounterparty.CounterpartyToJSON,
            'organization': AgentPolymorphicOrganization.OrganizationToJSON,
            'employee': AgentPolymorphicEmployee.EmployeeToJSON,
        };
        const converter = typeof discriminator === 'string'
            ? converters[discriminator] ?? converters['undefined']
            : undefined;
        if (converter !== undefined) {
            return converter(value);
        }
    }
    return {
        ...AgentPolymorphicParent.EntityWithMetaToJSONTyped(value as any, true),
    };
}

